const utilsFunctions = require('./utils.js')
const utils = new utilsFunctions()
const { Instance, connectDB } = require('./db')

async function speaker () {
 //TODO: Отправка случайных вопросов в чаты
    //чаты случайные, выгрузка из переменной phoneNumbers
    //вопросы случайные ( варианты: зготовка или gpt )
}
