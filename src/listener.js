const utilsFunctions = require('./utils.js')
const utils = new utilsFunctions()

async function listener() {
  //TODO: логика обработки вопросов поступающих в чаты ( так как отправка вопросов случайная необходимо их получать )
}
